generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// ユーザーを保存するためのテーブル定義（年齢を追加したぞ！）
model User {
  id   Int    @id @default(autoincrement())
  name String
  age  Int?
}
